base = [1, 4, -3, 0, 10]
baseNew = []
long = len(base)
longNew = len(baseNew)
minNum = base[0]
for numb in base:
    if numb < minNum:
        minNum = numb
baseNew.append(minNum)
while longNew < long:
    count = 0
    for i in long:
        if base[0] < base[i]:
            min = base[0]
        else
``  print(baseNew)

